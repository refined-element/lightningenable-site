node_modules/
.vercel/
.env
.env.*
!.env.example
*.log
.DS_Store
.vscode/
.idea/
dist/

# Local-only QA reference snapshots — generated for manual review,
# not served to visitors, and they tend to embed stale parameter
# names. Keep them in the working tree but out of git history.
audit/

