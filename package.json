{
  "name": "lightningenable-site",
  "version": "0.1.0",
  "description": "Marketing site for Lightning Enable (lightningenable.com). Static landing page + Vercel serverless functions for the embedded live demo (an autonomous agent settling real Lightning against the producer API).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "test": "node --test tests/"
  },
  "dependencies": {
    "l402-server": "^0.1.0",
    "l402-requests": "^0.1.0",
    "@noble/secp256k1": "^1.7.1",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vercel": "^53.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
